out/
__pycache__/
# autonomous-run Datom log (regenerated by methods/autorun.py — append-only local commit-DAG)
data/kanjo.datoms.kotoba.edn
data/ingest/
# scripts/refetch_edgar.cljs writes a same-run backup of the prior data file
# content before overwriting it — a local safety net, not a tracked artifact.
data/*.bak
